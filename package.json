{
  "name": "telegram-mcp-voice",
  "version": "1.1.0",
  "description": "MCP server that lets Claude send Telegram text and voice messages, deployable on Render via Docker.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "express": "^4.21.1",
    "zod": "^3.23.8"
  },
  "engines": {
    "node": ">=18"
  }
}
